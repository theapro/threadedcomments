export type Dictionary = {
  post: {
    joinconversation: string;
  };
  comment: {
    addComment: string;
    reply: string;
    cancel: string;
    noComments: string;
    share: string;
    report: string;
  };
};
